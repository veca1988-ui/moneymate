// TODO(revenuecat): Re-enable after adding purchases_flutter dependency
// and updating Xcode. Replace with actual RevenueCat initialization.
Future<void> initRevenueCat() async {
  // No-op until purchases_flutter is re-enabled in pubspec.yaml
}
